# SupabaseプロジェクトのURLとanon keyを設定する（Project Settings → API）。
# GitHub Pagesはビルド時に環境変数を埋め込む静的サイトなので、
# GitHub Actionsのsecretsにも同名で登録すること（.github/workflows/deploy.yml参照）。
PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
PUBLIC_SUPABASE_ANON_KEY=eyJxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
